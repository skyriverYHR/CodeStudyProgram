<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.github.skyriveryhr.springbootstudy.mapper.expenditure.DailyExpenseRecordsMapper">
    <select id="selectAll" resultType="DailyExpenseRecord">
        SELECT
            *
        FROM
            daily_expense_records
    </select>
</mapper>